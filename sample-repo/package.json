{
  "name": "sample-repo",
  "version": "1.0.0",
  "description": "Sample repository for testing component usage analyzer",
  "private": true,
  "workspaces": [
    "lib/*",
    "packages/*",
    "app/*"
  ],
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r dev"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
